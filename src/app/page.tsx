import {Section} from "@/components/section";

export default function Home() {
    return (
        <main className="flex-1">
            <Section id="hero" size="lg" className="bg-slate-50 dark:bg-gray-900">
                <div className="container">
                </div>
            </Section>
        </main>
    );
}
